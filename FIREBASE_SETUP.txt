PRIVATE HIRE BOOKING - FIREBASE STAFF + AUDIT SETUP (V3.6.0)

Your Firebase Web App configuration is already present in config.js.

REQUIRED UPDATE
1. Open Firebase Console > Firestore Database > Rules.
2. Replace the current rules with the complete contents of firestore.rules from this V3.6.0 package.
3. Click Publish.
4. Upload all V3.6.0 website files to GitHub Pages / Netlify and replace the previous build.
5. Sign in using the existing administrator account.
6. Open Staff Accounts to invite additional users.

Authentication > Email/Password must remain enabled.
Cloud Firestore must remain active.

The audit trail is stored under each workspace's /logs collection. Audit records are readable by active workspace members and append-only for authorised write roles.
